extends layout 

block content 
  h1= title

  ul 
    each instance in movieinstances_list 
      li 
        a(href=instance.url) #{instance.movie.name}
    else 
      li There are no movie instances.